name: Run Google Colab Notebook

on:
  schedule:
    - cron: '*/1 * * * *'  # Runs every minute
  workflow_dispatch:  # Allows manual triggering

jobs:
  run-colab:
    runs-on: ubuntu-latest  # Use the latest version of Ubuntu

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # Use the latest version

    - name: Install colab-cli and gitpython
      run: |
        pip install --upgrade pip  # Upgrade pip to the latest version
        pip install colab-cli gitpython  # Install required packages

    - name: Run Colab Notebook
      run: |
        colab-cli run --path https://colab.research.google.com/drive/1qbxBtj9oRab0QSOaxmbLxFzRYgI4cGmb?usp=sharing
